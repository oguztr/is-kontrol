{
  "name": "inventory-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/inventory-service/src",
  "projectType": "application",
  "tags": ["scope:inventory", "type:service"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "env": { "NODE_ENV": "production" },
        "cwd": "apps/inventory-service"
      },
      "configurations": {
        "development": { "env": { "NODE_ENV": "development" } }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "inventory-service:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": { "buildTarget": "inventory-service:build:development" },
        "production": { "buildTarget": "inventory-service:build:production" }
      }
    },
    "test": { "options": { "passWithNoTests": true } }
    ,
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json --emitDeclarationOnly --force",
        "cwd": "apps/inventory-service"
      }
    }
  }
}
