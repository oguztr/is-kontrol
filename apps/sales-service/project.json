{
  "name": "sales-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/sales-service/src",
  "projectType": "application",
  "tags": ["scope:sales", "type:service"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "env": { "NODE_ENV": "production" },
        "cwd": "apps/sales-service"
      },
      "configurations": {
        "development": { "env": { "NODE_ENV": "development" } }
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "sales-service:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": { "buildTarget": "sales-service:build:development" },
        "production": { "buildTarget": "sales-service:build:production" }
      }
    },
    "test": { "options": { "passWithNoTests": true } }
  }
}
